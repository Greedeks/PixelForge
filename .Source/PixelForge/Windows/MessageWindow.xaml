<Window x:Class="PixelForge.Windows.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:interop="clr-namespace:PixelForge.Helpers.Interop"
        xmlns:local="clr-namespace:PixelForge.Windows"
        mc:Ignorable="d"  Background="{StaticResource Background_Main}"   BorderBrush="{StaticResource Border_Default}" BorderThickness="1" interop:WindowAttributes.CaptionColor="{StaticResource Background_Page}" 
        FontFamily="{StaticResource AA Stetica}" Title="PixelForge" IsTabStop="False"  KeyboardNavigation.TabNavigation="None"  FocusVisualStyle="{x:Null}" SnapsToDevicePixels="True" UseLayoutRounding="True"
        ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        MinWidth="320" MinHeight="140" MaxWidth="480" MaxHeight="300">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,5">
            <TextBlock x:Name="TxtErrorDetails" TextWrapping="Wrap" FontSize="11" FontFamily="Consolas" Foreground="{StaticResource Primary_Accent_Text}" Opacity="0.75" />
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal"  HorizontalAlignment="Right">
            <Button x:Name="BtnCopy" Style="{StaticResource OutlinedActionButton}" Margin="0,0,6,0" Click="BtnCopy_Click">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="{StaticResource Icon_Copy}" Width="12" Height="12" Margin="0,0,6,0"/>
                    <TextBlock Grid.Column="1" Text="{DynamicResource btn_Copy}" Foreground="#FFFFFF" FontSize="12"/>
                </Grid>
            </Button>
            
            <Button x:Name="BtnClose" Style="{StaticResource OutlinedActionButton}" Content="{DynamicResource btn_OK}" IsDefault="True"  Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>