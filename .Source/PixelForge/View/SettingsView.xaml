<UserControl x:Class="PixelForge.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:uc="clr-namespace:PixelForge.Assets.UserControls"
             xmlns:vm="clr-namespace:PixelForge.Core.ViewModel"
             xmlns:local="clr-namespace:PixelForge.View"
             mc:Ignorable="d" Style="{StaticResource LaunchPage}" d:Width="800" d:Height="Auto">
    <UserControl.DataContext>
        <vm:SettingsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <uc:FlowPanel Orientation="Vertical" HorizontalAlignment="Stretch" ContentAlignment="Stretch" Margin="5" VerticalSpacing="7">
                <Grid>
                    <Border CornerRadius="8" Opacity="0.5" Background="{StaticResource Brush_ShadowOverlay}" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Border.RenderTransform>
                            <TranslateTransform X="3" Y="6" />
                        </Border.RenderTransform>
                    </Border>

                    <Border Background="{StaticResource Background_Card}" BorderBrush="{StaticResource Border_Default}" BorderThickness="1" CornerRadius="8" Padding="16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Image Height="20" Width="20" Source="{StaticResource Icon_Folder}"/>
                                <TextBlock Text="{DynamicResource title_OutputFolder}" Margin="8,0,0,0" Foreground="{StaticResource Primary_Accent_Text}" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Rectangle Height="1" Fill="{StaticResource Border_Default}" Margin="0,0,0,16"/>

                            <StackPanel>
                                <RadioButton IsChecked="{Binding IsNextToOriginal, Mode=TwoWay}" GroupName="OutputFolderGroup">
                                    <StackPanel>
                                        <WrapPanel>
                                            <TextBlock Text="{DynamicResource rbtn_NextToOriginal}" Foreground="{StaticResource Primary_Accent_Text}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                        </WrapPanel>
                                        <TextBlock Text="...\compressed\" Foreground="{StaticResource Inactive_Text}" FontSize="12" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton IsChecked="{Binding IsCustomFolder, Mode=TwoWay}" GroupName="OutputFolderGroup" Margin="0,12,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource rbtn_CustomFolder}" Foreground="{StaticResource Primary_Accent_Text}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                        <WrapPanel Margin="0,2,0,0">
                                            <TextBlock Foreground="{StaticResource Inactive_Text}" FontSize="12" TextWrapping="Wrap" TextTrimming="WordEllipsis">
                                                <Run Text="{Binding CustomFolderPath}"/>
                                                <Hyperlink Command="{Binding SelectFolderCommand}" Foreground="{StaticResource Accent_Text}">
                                                    <Run Text="{DynamicResource link_ChangeFolder}"/>
                                                </Hyperlink>
                                            </TextBlock>
                                        </WrapPanel>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton IsChecked="{Binding IsOverwriteOriginal, Mode=TwoWay}" GroupName="OutputFolderGroup" Margin="0,12,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource rbtn_OverwriteOriginal}" Foreground="{StaticResource Primary_Accent_Text}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                        <TextBlock Text="{DynamicResource desc_OverwriteOriginal}" Foreground="{StaticResource Inactive_Text}" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                    </StackPanel>
                                </RadioButton>

                                <Rectangle Height="1" Fill="{StaticResource Border_Default}" Margin="0,16,0,16"/>

                                <DockPanel LastChildFill="True">
                                    <Image Height="20" Width="20" DockPanel.Dock="Left" VerticalAlignment="Center">
                                        <Image.Style>
                                            <Style TargetType="{x:Type Image}">
                                                <Setter Property="Source" Value="{StaticResource Icon_Info}"/>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <TextBlock Text="{DynamicResource desc_TempFilesFallback}" Margin="8,0,0,0" Foreground="{StaticResource Primary_Accent_Text}"  FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                </DockPanel>

                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid>
                    <Border CornerRadius="8" Opacity="0.5" Background="{StaticResource Brush_ShadowOverlay}" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Border.RenderTransform>
                            <TranslateTransform X="3" Y="6" />
                        </Border.RenderTransform>
                    </Border>

                    <Border Background="{StaticResource Background_Card}" BorderBrush="{StaticResource Border_Default}" BorderThickness="1" CornerRadius="8" Padding="16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <Image Height="20" Width="20">
                                    <Image.Style>
                                        <Style TargetType="{x:Type Image}">
                                            <Setter Property="Source" Value="{StaticResource Icon_Import}"/>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="{DynamicResource title_XamlConversion}" Margin="8,0,0,0" Foreground="{StaticResource Primary_Accent_Text}" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Rectangle Height="1" Fill="{StaticResource Border_Default}" Margin="0,0,0,16"/>

                            <StackPanel>
                                <RadioButton IsChecked="{Binding IsOptimizeImmediately, Mode=TwoWay}" GroupName="ImportSVG">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource rbtn_OptimizeImmediately}" Foreground="{StaticResource Primary_Accent_Text}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                        <TextBlock Text="{DynamicResource desc_OptimizeImmediately}" Foreground="{StaticResource Inactive_Text}" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton  IsChecked="{Binding IsKeepOriginal, Mode=TwoWay}" GroupName="ImportSVG" Margin="0,12,0,0">
                                    <TextBlock Text="{DynamicResource rbtn_KeepOriginal}" Foreground="{StaticResource Primary_Accent_Text}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                                </RadioButton>

                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
                
            </uc:FlowPanel>
        </ScrollViewer>
    </Grid>
</UserControl>